# ============================================================
# WAR ARCHIVE - Docker Compose 설정
# front: NAS WebStation에서 직접 서비스 (Docker 밖)
# back, mysql, nginx: Docker 컨테이너로 운영
# ============================================================

services:
  # ── MySQL 데이터베이스 ──
  mysql:
    image: mysql:8.0
    container_name: wararchive-mysql
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
      MYSQL_DATABASE: ${DB_NAME}
      MYSQL_USER: ${DB_USER}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    volumes:
      - mysql_data:/var/lib/mysql
      - ../data/db:/docker-entrypoint-initdb.d:ro
    networks:
      - wararchive-net
    deploy:
      resources:
        limits:
          memory: 512M
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 30s

  # ── Backend API 서버 ──
  backend:
    build:
      context: ../back
      dockerfile: Dockerfile
    container_name: wararchive-backend
    restart: unless-stopped
    read_only: true
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp
    depends_on:
      mysql:
        condition: service_healthy
    deploy:
      resources:
        limits:
          memory: 256M
    environment:
      NODE_ENV: production
      PORT: 3000
      DB_HOST: mysql
      DB_PORT: 3306
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
      JWT_SECRET: ${JWT_SECRET}
      CORS_ORIGIN: ${CORS_ORIGIN}
      LOG_DIR: /app/data/logs
      UPLOAD_DEST: /app/data/uploads
      PRIVATE_JSON_DIR: /app/data/private-json
    volumes:
      - ../data/logs:/app/data/logs
      - ../data/uploads:/app/data/uploads
      - ../data/private-json:/app/data/private-json
      - ../data/cache:/app/data/cache
    networks:
      - wararchive-net

  # ── Nginx 리버스 프록시 (API용) ──
  nginx:
    image: nginx:alpine
    container_name: wararchive-nginx
    restart: unless-stopped
    read_only: true
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp
      - /var/cache/nginx
      - /run
    depends_on:
      - backend
    ports:
      - "${NGINX_PORT:-8080}:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - wararchive-net
    deploy:
      resources:
        limits:
          memory: 128M

volumes:
  mysql_data:
    driver: local

networks:
  wararchive-net:
    driver: bridge
