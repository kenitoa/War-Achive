{
  "name": "war-archive-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal NAS-ready static backend for War Archive.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "dependencies": {
    "mysql2": "^3.14.0"
  },
  "engines": {
    "node": ">=20"
  }
}
